<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Text IGNITD</title>
  <meta name="robots" content="noindex" />
  <style>
    body{margin:0;min-height:100vh;display:flex;align-items:center;justify-content:center;font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;background:#0f0f0f;color:#f4f4f5}
    .card{max-width:520px;width:92%;background:#18181b;border:1px solid #27272a;border-radius:20px;padding:28px;text-align:center;box-shadow:0 0 30px rgba(197,151,89,.15)}
    .brand{color:#c59759;font-weight:800;letter-spacing:.08em;font-size:34px;margin-bottom:10px}
    h1{margin:0 0 10px;font-size:28px}
    p{margin:0 0 18px;color:#a1a1aa;line-height:1.4}
    a.btn{display:inline-block;background:#c59759;color:#111827;text-decoration:none;font-weight:700;padding:14px 18px;border-radius:12px}
    .sub{margin-top:14px;font-size:12px;color:#71717a}
  </style>
</head>
<body>
  <div class="card">
    <div class="brand">IGNITD</div>
    <h1>Text to Connect</h1>
    <p>Tap below to open a text to <strong>(702) 885 8008</strong>.</p>

    <a id="smsLink" class="btn" href="sms:7028858008">Open Text Message</a>

    <div class="sub">If nothing opens, you’re probably on desktop. Try from your phone.</div>
  </div>

  <script>
    (function () {
      const phone = "7028858008";
      const message = "Learn more about IGNITD!";
      const body = encodeURIComponent(message);
      const isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent);

      const href = isIOS
        ? `sms:${phone}&body=${body}`
        : `sms:${phone}?body=${body}`;

      const link = document.getElementById("smsLink");
      link.href = href;

      // Optional: auto-try once on load (some browsers will ignore without a click)
      // window.location.href = href;
    })();
  </script>
</body>
</html>
